# 扩展测试指南

## 安装和测试步骤

### 1. 加载扩展
1. 打开 Chrome 浏览器
2. 访问 `chrome://extensions/`
3. 开启"开发者模式"
4. 点击"加载已解压的扩展程序"
5. 选择项目的 `dist` 目录

### 2. 测试弹窗功能
1. 点击浏览器工具栏中的扩展图标
2. 弹窗应该正常显示，不再出现无限loading
3. 首次使用时设置主密码
4. 验证登录功能是否正常

### 3. 测试密码管理功能
1. 添加一个测试密码
2. 验证密码列表显示
3. 测试密码复制功能
4. 测试密码编辑和删除功能

### 4. 测试自动填充功能
1. 访问一个登录页面（如 GitHub、Gmail 等）
2. 点击扩展图标
3. 选择要填充的密码
4. 验证表单是否正确填充

## 预期结果

✅ **弹窗正常加载**：不再出现loading卡住的问题
✅ **身份验证正常**：可以设置和验证主密码
✅ **密码管理正常**：可以添加、编辑、删除密码
✅ **复制功能正常**：可以复制密码到剪贴板
✅ **自动填充正常**：可以在网页中自动填充密码

## 故障排除

如果仍然遇到问题：

1. **检查控制台错误**：
   - 右键点击扩展图标 → 检查弹窗
   - 查看是否有JavaScript错误

2. **检查后台脚本**：
   - 在 `chrome://extensions/` 中点击"检查视图：背景页"
   - 查看后台脚本的错误信息

3. **重新构建**：
   ```bash
   npm run build
   ```

4. **重新加载扩展**：
   - 在扩展管理页面点击刷新按钮

## 技术验证点

- [x] Webpack正确打包所有模块
- [x] CryptoUtils正确集成到background.js
- [x] 弹窗通过消息传递调用解密功能
- [x] Content script正确处理表单填充
- [x] 所有模块使用统一的加密解密方法

修复完成后，扩展应该能够正常工作，不再出现loading问题。